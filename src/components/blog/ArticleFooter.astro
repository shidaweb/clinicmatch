---
import Button from '~/components/ui/Button.astro';
import FAQPageJsonLd from '~/components/seo/FAQPageJsonLd.astro';
import RelatedPosts from '~/components/blog/RelatedPosts.astro';
import type { Post } from '~/types';
import { getPermalink } from '~/utils/permalinks';

export interface FaqItem {
  q: string;
  a: string;
}

interface Props {
  post: Post;
  faqs?: FaqItem[];
}

const { post, faqs = post.faqs ?? [] } = Astro.props;

const FORM_SELL = getPermalink('contact/sell');
const FORM_BUY = getPermalink('contact/buy');
const LINE_URL = 'https://lin.ee/vepVhWc';
---

<section class="mx-auto px-6 sm:px-6 max-w-3xl mt-12 pt-10 border-t border-line" aria-labelledby="article-footer-heading">
  <h2 id="article-footer-heading" class="text-xl font-bold font-heading text-plum mb-6">
    この記事に関するご相談
  </h2>

  <div class="flex flex-wrap gap-4 mb-6">
    <Button href="/cases?view=active" variant="secondary">在庫をさがす</Button>
    <Button href={FORM_SELL} variant="primary">売却を相談する</Button>
    <Button href={FORM_BUY} variant="secondary">購入を相談する</Button>
    <Button href={LINE_URL} variant="line" target="_blank" rel="noopener noreferrer">LINEで相談</Button>
  </div>

  {
    faqs.length > 0 && (
      <>
        <FAQPageJsonLd faqs={faqs} />
        <div class="mb-10">
          <h3 class="text-lg font-bold font-heading text-plum mb-4">よくある質問</h3>
          <dl class="space-y-4">
            {faqs.map((faq) => (
              <div class="cm-card p-4">
                <dt class="font-semibold text-plum">{faq.q}</dt>
                <dd class="mt-2 text-plum-soft text-sm leading-relaxed">{faq.a}</dd>
              </div>
            ))}
          </dl>
        </div>
      </>
    )
  }

  <RelatedPosts post={post} />
</section>
