<section class="membership-section border-y border-line bg-ivory">
  <div class="max-w-content mx-auto px-4 sm:px-6">
    <header class="membership-heading">
      <h2 class="cm-heading text-xl md:text-2xl font-semibold">売却・購入の希望を登録する</h2><p>
        投稿内容は運営が確認してから公開します。組織名や連絡先は公開されません。
      </p>
    </header>
    <div class="membership-options">
      <article>
        <h3>機器を売りたい</h3><p>
          メーカー・機種名、状態、希望価格を登録します。公開後に関心が寄せられた際は、メールでお知らせします。
        </p><a href="/auth/register?trade_side=sell" class="cm-btn-primary">売りたい機器を登録する</a><a
          href="/cases?view=active&type=wanted"
          class="membership-secondary">買い手の希望を見る →</a
        >
      </article>
      <article>
        <h3>機器を買いたい</h3><p>
          探している機種、予算、導入時期を登録します。公開後に売り手から提案が届いた際は、メールでお知らせします。
        </p><a href="/auth/register?trade_side=buy" class="cm-btn-secondary">購入希望を登録する</a><a
          href="/cases?view=active"
          class="membership-secondary">販売中の機器を見る →</a
        >
      </article>
    </div>
    <div class="membership-consult">
      <p>登録前の相談も受け付けています。</p><div>
        <a href="/contact">フォームで相談する →</a><a
          href="https://lin.ee/vepVhWc"
          target="_blank"
          rel="noopener noreferrer">LINEで相談する ↗</a
        >
      </div>
    </div>
  </div>
</section>
<style>
  .membership-section {
    padding: 56px 0;
  }
  .membership-heading p {
    margin-top: 12px;
    line-height: 1.8;
    color: var(--plum-soft);
    font-size: 15px;
  }
  .membership-options {
    display: grid;
    grid-template-columns: 1fr 1fr;
    margin-top: 32px;
    gap: 40px;
  }
  .membership-options article + article {
    border-left: 1px solid var(--line);
    padding-left: 40px;
  }
  .membership-options h3 {
    font-size: 18px;
    font-weight: 700;
  }
  .membership-options p {
    margin: 14px 0 24px;
    line-height: 1.9;
    color: var(--plum-soft);
    font-size: 15px;
    max-width: 36ch;
  }
  .membership-secondary {
    display: block;
    margin-top: 14px;
    font-size: 14px;
    color: var(--rose-deep);
    padding: 6px 0;
  }
  .membership-consult {
    display: flex;
    justify-content: space-between;
    gap: 20px;
    margin-top: 32px;
    padding-top: 24px;
    border-top: 1px solid var(--line);
    font-size: 14px;
  }
  .membership-consult div {
    display: flex;
    flex-wrap: wrap;
    gap: 12px 24px;
  }
  .membership-consult a {
    color: var(--rose-deep);
  }
  @media (max-width: 639px) {
    .membership-section {
      padding: 40px 0;
    }
    .membership-options {
      grid-template-columns: 1fr;
      gap: 28px;
    }
    .membership-options article + article {
      border-left: 0;
      border-top: 1px solid var(--line);
      padding: 28px 0 0;
    }
    .membership-consult {
      flex-direction: column;
    }
  }
</style>
