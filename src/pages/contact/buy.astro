---
import Layout from '~/layouts/PageLayout.astro';
import WidgetWrapper from '~/components/ui/WidgetWrapper.astro';
import BuyForm from '~/components/interactive/BuyForm';
import Breadcrumb from '~/components/seo/Breadcrumb.astro';
import { getPermalink, getHomePermalink } from '~/utils/permalinks';

export const prerender = true;

const metadata = {
  title: '購入希望フォーム',
  description: '中古美容医療機器の購入をご希望の方はこちらのフォームからお問い合わせください。',
};
---

<Layout metadata={metadata}>
  <WidgetWrapper containerClass="max-w-2xl mx-auto">
    <!-- パンくず: トップ > お問い合わせ > 購入希望 -->
    <Breadcrumb
      class="mb-6"
      items={[
        { name: 'トップ', url: getHomePermalink() },
        { name: 'お問い合わせ', url: getPermalink('contact') },
        { name: '購入希望' },
      ]}
    />

    <header class="mb-8">
      <h1 class="text-2xl md:text-3xl font-bold text-text mb-2">購入希望フォーム</h1>
      <p class="text-text-muted text-sm">入力項目と所要時間（約2分）・匿名OK</p>
      <div class="mt-4 h-1.5 rounded-full bg-surface-muted overflow-hidden">
        <div class="h-full w-1/3 rounded-full bg-buy" aria-hidden="true"></div>
      </div>
    </header>

    <BuyForm client:load />
  </WidgetWrapper>
</Layout>
